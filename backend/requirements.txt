# Core
python-dotenv==1.0.1
pydantic>=2.11

# LLM + embeddings (OpenAI) — unpinned minor/patch so pip can resolve a
# mutually-compatible set instead of a dependency conflict
langchain>=1.3
langchain-openai>=1.4
langchain-core>=1.5
langchain-text-splitters>=1.1
langchain-community>=0.4

# Orchestration
langgraph>=1.2

# Vector store
langchain-qdrant>=1.1
qdrant-client>=1.18

# Data agent (F5)
sqlalchemy>=2.0.51

# Code agent sandboxing (F6) — added when we build that feature

# Web agent (F4, optional)
tavily-python==0.5.0

# Evaluation (F11, added in Phase 4)
ragas==0.2.6

# Observability (F12, added in Phase 5)
langfuse==2.53.9

# API server
fastapi==0.115.5
uvicorn==0.32.1